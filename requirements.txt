icpp-pro>=5.5.1
icp-py-core==2.3.0

# Pin cbor2<6: cbor2 6.x decodes tag-55799-wrapped maps to frozendict, which
# breaks icp-py-core's isinstance(response, dict) check.
# See https://github.com/eliezhao/icp-py-core/issues/14.
cbor2<6

# to lint python scripts — pinned to the same versions used by icpp-pro
# after its Python 3.10–3.14 matrix bump. Keeping all three repos on the
# same toolchain avoids style/check drift. Older pylint+astroid crashed
# on Python 3.12+ syntax (PEP 695 `type X = ...`).
black==26.3.1
pylint==3.3.4
mypy==1.13.0